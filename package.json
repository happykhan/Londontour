{
  "name": "londontour",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "npx --yes serve .",
    "deploy": "npx --yes vercel deploy --prod --yes --scope happykhans-projects"
  }
}
